{
    "extends": "../tsconfig",
    "compilerOptions": {
        "baseUrl": "src",
        "composite": true,
        "types": ["vitest/globals"] // Tell TypeScript that vitest is available without an import
    },
    "include": ["src", "vite.config.ts"]
}
